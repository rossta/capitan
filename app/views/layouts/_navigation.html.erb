<div id="navbar" class="navbar">
  <div class="container">
    <header class="navigation-header">
      <h3 class="left"><%= link_to 'capitan', root_path %></h3>
    </header>
    <nav class="right">
      <ul class="nav">
        <% if signed_in? %>
          <li><%= link_to "rossta", current_user.url %></li>
          <li><%= link_to "logout", sign_out_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</div>
