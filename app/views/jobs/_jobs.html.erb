<div id="jobs">
  <%= page_header("Jobs") %>
  <ul class="unstyled">
    <% @jobs.each do |job| %>
      <li>
        <h2><%= link_to job.display_name, job %></h2>
        <%= render job %>
      </li>
    <% end %>
  </ul>
</div>
